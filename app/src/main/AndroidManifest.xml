<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jjoey.transportr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ClientHomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.VerifyOTPActivity" />
        <activity android:name=".activities.AuthActivity" />
        <activity android:name=".activities.LandingActivity" />
        <activity android:name=".activities.BookingHistoryActivity" />
        <activity android:name=".activities.PaymentsActivity" />
        <activity android:name=".activities.NotificationsActivity" />
        <activity android:name=".activities.HelpActivity" />
        <activity android:name=".activities.ReferActivity" />
        <activity android:name=".activities.PrivacyPolicyActivity" />
        <activity android:name=".activities.ProfileSettingsActivity" />
        <activity android:name=".activities.BookRideActivity"></activity>
    </application>

</manifest>